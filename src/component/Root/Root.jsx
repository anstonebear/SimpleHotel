import { BrowserRouter } from "react-router-dom";

import Routing from "../Routing/Routing";

export const Root = () => (
  <BrowserRouter>
    <Routing />
  </BrowserRouter>
);

export default Root;
